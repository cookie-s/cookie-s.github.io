title: "site-title"
# email: your-email@domain.com
description: > # this means to ignore newlines until "baseurl:"
  This is Cookies.

# Build settings
markdown: kramdown
# theme: minima
gems:
exclude:
  - Gemfile
  - Gemfile.lock
